import { Module } from '@nestjs/common';

/**
 * Bounded context: Ajuda/Suporte (SDD.md §2.1).
 * Requisitos cobertos: RF-16, RN-13.
 * Tarefas de implementação: BE-37 (TASK.md §3.9).
 *
 * Módulo intencionalmente vazio nesta tarefa (BE-01) — ver nota em
 * `identity-access.module.ts`.
 */
@Module({})
export class AjudaSuporteModule {}
