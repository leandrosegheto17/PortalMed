import { Module } from '@nestjs/common';

/**
 * Bounded context: Auditoria (SDD.md §2.1).
 * Requisitos cobertos: RF-10, RN-08, RNF-04.
 * Tarefas de implementação: BE-29 a BE-31 (TASK.md §3.6).
 *
 * Módulo intencionalmente vazio nesta tarefa (BE-01) — ver nota em
 * `identity-access.module.ts`.
 */
@Module({})
export class AuditoriaModule {}
