import { Module } from '@nestjs/common';

/**
 * Bounded context: Cadastro & Consentimento (SDD.md §2.1).
 * Requisitos cobertos: RF-12, RF-15, RN-01, RN-02, RN-14.
 * Tarefas de implementação: BE-18, BE-19 (TASK.md §3.3).
 *
 * Módulo intencionalmente vazio nesta tarefa (BE-01) — ver nota em
 * `identity-access.module.ts`.
 */
@Module({})
export class CadastroConsentimentoModule {}
