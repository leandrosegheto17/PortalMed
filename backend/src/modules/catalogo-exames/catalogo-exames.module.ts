import { Module } from '@nestjs/common';

/**
 * Bounded context: Catálogo de Exames (SDD.md §2.1).
 * Requisitos cobertos: RF-05.
 * Tarefas de implementação: BE-20 (TASK.md §3.4).
 *
 * Módulo intencionalmente vazio nesta tarefa (BE-01) — ver nota em
 * `identity-access.module.ts`.
 */
@Module({})
export class CatalogoExamesModule {}
