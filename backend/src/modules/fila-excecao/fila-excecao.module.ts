import { Module } from '@nestjs/common';

/**
 * Bounded context: Fila de Exceção (SDD.md §2.1).
 * Requisitos cobertos: RF-14.
 * Tarefas de implementação: BE-24, BE-25, BE-38 (TASK.md §3.4).
 *
 * Módulo intencionalmente vazio nesta tarefa (BE-01) — ver nota em
 * `identity-access.module.ts`.
 */
@Module({})
export class FilaExcecaoModule {}
