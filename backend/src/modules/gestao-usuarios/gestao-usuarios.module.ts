import { Module } from '@nestjs/common';

/**
 * Bounded context: Gestão de Usuários (Admin) (SDD.md §2.1).
 * Requisitos cobertos: RF-13.
 * Tarefas de implementação: BE-35 (TASK.md §3.8).
 *
 * Módulo intencionalmente vazio nesta tarefa (BE-01) — ver nota em
 * `identity-access.module.ts`.
 */
@Module({})
export class GestaoUsuariosModule {}
